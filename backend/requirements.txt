# FastAPI and server
fastapi>=0.109.0,<0.120.0
uvicorn[standard]>=0.27.0,<0.35.0
python-multipart>=0.0.6,<0.1.0

# Image processing
opencv-python-headless==4.10.0.84
Pillow>=10.2.0,<12.0.0
numpy>=1.24.0,<2

# OCR - EasyOCR (PyTorch-based, CPU-only)
# Pinned for stability - EasyOCR uses PyTorch internally
easyocr==1.7.2
# PyTorch CPU-only wheel (smaller, no CUDA)
torch>=2.0.0,<2.6.0
torchvision>=0.15.0,<0.21.0

# Validation and utilities
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Fuzzy matching (for brand name comparison)
rapidfuzz>=3.6.0,<4.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
